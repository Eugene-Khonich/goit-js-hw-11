export const greateCards = imgInfo => {
  const imgValues = imgInfo.map(
    ({
      webformatURL,
      largeImageURL,
      tags,
      likes,
      views,
      comments,
      downloads,
    }) => {}
  )
  return;
};
